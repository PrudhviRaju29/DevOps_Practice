name: DevOps Pracice Workflow
on: [push]
jobs:
 build:
   runs-on: ubuntu-latest
   steps:
    - name: checkout code
    - uses: actions/checkout@v3
    - name: Print Hello
    - run: echo "Github Actions is working, Prudhvi!"
